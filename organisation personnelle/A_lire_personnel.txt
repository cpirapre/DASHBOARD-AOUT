checklist de choses à faire:

1. Faire le schéma conceptuel
2. Quand il y a des nouveaux logs, il faut pouvoir les entrer dans la base de données
de manière efficace.
3. Il faut une relation de QuestionAsked à QuestionAsked pour dire quelle question suit
laquelle
4. Il faut le code de la base de données
5. Il faut faire le diagramme des appels de fonctions





commande pour créer un environnement maven:
mvn archetype:generate -DarchetypeArtifactId=maven-archetype-quickstart -DarchetypeVersion=1.1
compilation: mvn package
pour lancer l'application: java -cp target/ernest_stat-1.0.SNAPSHOT.jar
